resources:
- new-deployment.yaml

configMapGenerator:
- name: new-config
  literals:
  - welcomeMessage=Welcome to pod 4

secretGenerator:
- name: new-secret
  literals:
  - username=admin
  - password=password@123

generatorOptions:
  disableNameSuffixHash: false
